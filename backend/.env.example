PORT=3000
AUTH_MODE=jwt
JWT_SECRET=local-development-secret
DATABASE_URL=Server=localhost,1433;Database=cmdb_dev;User Id=sa;Password=YourStrong!Passw0rd;Encrypt=true;TrustServerCertificate=true;
CORS_ORIGIN=http://localhost:4200

# Local SQL Server setup:
# 1) docker compose -f docker-compose.sql.yml up -d
# 2) npm run db:bootstrap
